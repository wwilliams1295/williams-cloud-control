services:
  - type: web
    name: williams-cloud-control-test
    env: python
    plan: free
    buildCommand: pip install fastapi uvicorn
    startCommand: uvicorn test_app:app --host 0.0.0.0 --port $PORT



